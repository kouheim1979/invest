/* Private data goes outside the public repository. stdin: validated statement packet. */
'use strict';
const fs=require('node:fs'),path=require('node:path'),zlib=require('node:zlib'),C=require('../broker-receipts-core.js');
const target=path.resolve(process.argv[2]||''),repo=path.resolve(__dirname,'..');
if(!process.argv[2]||target===repo||target.startsWith(repo+path.sep))throw Error('Write private handoff files outside the public repository.');
const packet=C.validate(JSON.parse(fs.readFileSync(0,'utf8'))),json=JSON.stringify(packet),token=zlib.gzipSync(json,{level:9}).toString('base64url');
const url='https://kouheim1979.github.io/invest/broker-receipts.html#br1='+token;
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const yen=n=>n.toLocaleString('ja-JP')+'円',t=C.total(packet.receipts),docs=new Map(packet.documents.map(d=>[d.id,d]));
const annual=C.years(packet).map(y=>'<tr><th>'+y.year+'年'+(y.coverage==='partial'?'（一部）':'')+'</th>'+['total','dividend','substitute','interest'].map(k=>'<td>'+yen(y[k])+'</td>').join('')+'</tr>').join('');
const rows=packet.receipts.slice().reverse().map(r=>'<tr><td>'+r.date+'</td><td>'+esc(C.labels[r.category])+'</td><td>'+esc(r.instrument||'—')+'</td><td>'+yen(r.amount)+'</td><td>'+r.sources.map(s=>esc(docs.get(s.documentId).name)+(s.page===null?'':' '+s.page+'ページ')+(s.line===null?'':' '+s.line+'行')).join('<br>')+'</td></tr>').join('');
const html=`<!doctype html><html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="referrer" content="no-referrer"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'"><title>資産コンパス 証券実績 ${packet.preparedAt}</title><style>html{font:15px/1.75 system-ui,sans-serif;background:#09131f;color:#edf3f8}body{max-width:1050px;margin:auto;padding:24px 18px}h1{font-size:25px}h2{font-size:18px;margin-top:28px}p{color:#b5c8d8}strong.total{display:block;font-size:36px;color:#90eddb}a{color:#7fe5e0}.apply{display:block;text-align:center;padding:17px;background:#7fe5e0;color:#08252b;font-weight:700;text-decoration:none;border-radius:12px;margin:22px 0}table{border-collapse:collapse;width:100%;font-size:13px}td,th{text-align:right;padding:11px 12px;border-bottom:1px solid #29445a;white-space:nowrap}th:first-child,td:first-child{text-align:left}.wrap{overflow:auto}summary{cursor:pointer;margin:20px 0}small{color:#a6bbce}.scope{padding:15px;border:1px solid #36596b;border-radius:12px}</style></head><body><h1>証券実績 · 配当金と貸株</h1><p class="scope">${esc(packet.scopeNote)}</p><h2>受取合計（配当＋貸株）</h2><strong class="total">${yen(t.total)}</strong><p>${t.count}件・${packet.documents.length}資料。証券明細の受取金額です。</p><a class="apply" href="${url}" target="_blank" rel="noopener noreferrer">資産コンパスへ反映する</a><p>普段使うブラウザで開き、「この端末へ反映する」を押してください。その後は資産コンパスの「配当 → 証券実績」で見られます。</p><h2>年別の受取実績</h2><div class="wrap"><table><thead><tr><th>年</th><th>受取合計</th><th>配当金</th><th>貸株配当金相当額</th><th>貸株金利</th></tr></thead><tbody>${annual}</tbody></table></div><p>貸株配当金相当額と貸株金利も合計に含めています。銀行への振替は重複計上していません。一部確認の年は通年との単純比較ができません。</p><details><summary>全${t.count}件の明細と出典を見る</summary><div class="wrap"><table><thead><tr><th>受取日</th><th>区分</th><th>銘柄</th><th>受取額</th><th>出典</th></tr></thead><tbody>${rows}</tbody></table></div></details><p><a href="data:application/json;base64,${Buffer.from(json).toString('base64')}" download="資産コンパス_証券実績_${packet.preparedAt}.json">バックアップJSONを保存</a></p><small>このファイルと反映リンクには金融データが含まれます。ご本人用に保管してください。</small></body></html>`;
fs.mkdirSync(path.dirname(target),{recursive:true});fs.writeFileSync(target,html);console.log(JSON.stringify({output:target,rows:t.count,documents:packet.documents.length,urlLength:url.length,total:t.total}));
