name: Patch portfolio colors and fonts

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy one-time patch disabled; current styles are already in app-v3.html.'
